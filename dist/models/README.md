# Temporary Model Asset

`dogdog.glb` is the current model loaded by the AR scene.

Source and license: project-provided asset. Confirm usage rights before publishing.

Replace this file with the final production `.glb` model before publishing.
Keep the filename as `dogdog.glb`, or update `src/config.ts`.
